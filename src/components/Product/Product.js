import React from 'react';

const Product = (props) => {
    const {id, img, name, price, ratings} = props.product
    return (
        <div>
            <h1>hello</h1>
        </div>
    );
};

export default Product;